= simple_form_for @card do |f|
  = f.input :original_text, label: t('card.original_text')
  = f.input :translated_text, label: t('card.translated_text')
  = f.button :submit, t('button.create_update')
